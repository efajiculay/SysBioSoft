#The following script are optional
sudo apt update
sudo apt install software-properties-common
#sudo add-apt-repository ppa:deadsnakes/ppa   -might not be needed in most cases

#If python3.7 or higher is not installed (normally already have 3.8 by default)
sudo apt install python3
sudo apt-get install python3-tk
sudo apt install python3-distutils

#Installing pip here
sudo apt install curl
curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
python3 get-pip.py

#Installing common lissing libraries
python3 -m pip install DateTime
python3 -m pip install pathlib
python3 -m pip install Pillow
python3 -m pip install numpy
python3 -m pip install thread6
python3 -m pip install pandas
python3 -m pip install matplotlib==3.3.3
python3 -m pip install python-libsbml
python3 -m pip install scipy
python3 -m pip install sdeint
python3 -m pip install func_timeout
python3 -m pip install sympy

#Installing BioSANS
python3 -m pip install -i https://test.pypi.org/simple/ BioSANS2020-efajiculay

#Locating BioSANS installation directory
python3 -m pip install show BioSANS2020-efajiculay

#Save the result of the above command for you will need






